// Press ALT + Z to fit all the text in the screen

// Declare two variables, one with const and one with let
// Give each one any value you'd like (string, number, array, object, boolean)
// const num1 = 420
// let num2 = 30
// Log the values of each one of them to the console
// console.log(sentence,number)

// Create an ES5 function with no parameters that prints a string to the console

// function myFunction () {
//   if (number === 420)
//   console.log("it worked: ",sentence, number);
// }
// myFunction()

// Create an ES5 function that takes 2 parameters and **RETURNS** a string that 
// includes those 2 parameters using template literal. Then log the value of that function to the console.

// function myParamFunction (a,b) {
//   return (`what ${a} you ${b}?`)
// }

// console.log(myParamFunction("do","mean"));

// Transform the 2 ES5 functions from the previous exercises into ES6

// let myParamFunction = (a,b) => a*b;
// console.log(myParamFunction(3,2))

// let myFunction = (a,b) => a+b 
//   console.log(myFunction(1,2))

// let myParamFunction = (a,b) => `what ${a} you ${b}?`
//   console.log(myParamFunction("do","mean"));

// Create an array containing 5 elements.

// let myArray = [1,2,3,4,5,17]

// Log the first 2 values to the console

// console.log(myArray[0,1])

// Log the last element in the array to the console

// console.log(myArray[myArray.length -1])

// Log elements 3 and 4 in the array to the console

// console.log(myArray[2,3])

// Create an object with the keys "name", "age", "occupation" and "hobbies" 
// (hobbies should be an array)

// const jsStudent = {
//   name: "Daniel",
//   age: 35,
//   occupation: "Student",
//   hobbies: ["Music","Learning","Beer"],
// }

// Log the name to the console
 
// console.log(jsStudent.name)

// Log the occupation and age to the console

// console.log(jsStudent.occupation)

// Log all the hobbies to the console

// console.log(jsStudent.hobbies)

// Log only the first hobby to the console

// console.log(jsStudent.hobbies[0])

// Create an array containing 3 objects. The objects should have the same keys as the 
// one in the previous exercise

const objects = [
  const student = {
    name: "Daniel",
    age: 35,
    occupation: "Student",
    hobbies: ["Music","Learning","Beer"]
  },
  const teacher = {
    name: "Reagan",
    age: 30,
    occupation: "teacher",
    hobbies: ["Sport","Teaching","Cocktails"]
  },
  const wife = {
    name: "Lena",
    age: 31,
    occupation: "Architect",
    hobbies: ["Music","Couching","Wine"]
  }
]

// Loop through the array of objects and log all the names to the console

// Give 2 of your objects the same age. Now filter only the objects with that age.

// Use the find method to display the last person in the array to the console.
